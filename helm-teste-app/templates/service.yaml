apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-teste-app.fullname" . }}
  labels:
    app: {{ template "helm-teste-app.name" . }}
    chart: {{ template "helm-teste-app.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-teste-app.name" . }}
    release: {{ .Release.Name }}