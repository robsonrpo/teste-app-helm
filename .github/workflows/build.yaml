name: Teste CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.listar-juntas.outputs.teste }}
      output2: ${{ steps.listar-juntas.outputs.test }}

    steps:
    - id: listar-juntas
      run: |
        set -x
        commit_message=$(echo "${{ github.event.head_commit.message }}" | grep "\[ambientes\]")
        echo ${commit_message##*:}

        JSON_STRING=$(echo ${commit_message##*:} | tr ',' '\n' | jq -R . | jq -s .)
        echo JSON_STRING2=\'$JSON_STRING\'
        echo "teste=JSON_STRING2">> "$GITHUB_OUTPUT"
        echo "test=world" >> "$GITHUB_OUTPUT"
        echo "$GITHUB_OUTPUT"

  build2:

    runs-on: ubuntu-latest
    needs: build
    steps:
    - env:
        OUTPUT1: ${{needs.build.outputs.output1}}
        OUTPUT2: ${{needs.build.outputs.output2}}
      run: |
        teste="$OUTPUT1"
        echo "teste=$teste"
        echo "test=$OUTPUT2"

  job1:
    runs-on: ubuntu-latest
    # Map a step output to a job output
    outputs:
      output1: ${{ steps.step1.outputs.test }}
      output2: ${{ steps.step2.outputs.test }}
    steps:
      - id: step1
        run: echo "test=hello" >> "$GITHUB_OUTPUT"
      - id: step2
        run: echo "test=world" >> "$GITHUB_OUTPUT"
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - env:
          OUTPUT1: ${{needs.job1.outputs.output1}}
          OUTPUT2: ${{needs.job1.outputs.output2}}
        run: echo "$OUTPUT1 $OUTPUT2"

